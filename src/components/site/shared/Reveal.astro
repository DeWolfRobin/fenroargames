---
interface Props {
  delay?: number;
  class?: string;
}

const { delay = 0, class: className = '' } = Astro.props;
---

<div class:list={['reveal-block', className]} style={{ '--reveal-delay': `${delay}ms` }}>
  <slot />
</div>
